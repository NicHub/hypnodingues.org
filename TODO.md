# À faire

## Typographie

- [ ] `hugo.yaml`, champ `params.description` — remplacer `d'hypnose` par `d’hypnose`.
- [ ] `hugo.yaml`, champ `markup.goldmark.extensions.footnote.backlinkHTML` — remplacer les délimiteurs simples dans `backlinkHTML: '↩︎'` par des délimiteurs doubles : `backlinkHTML: "↩︎"`.
