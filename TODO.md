# À faire

## Typographie

- [x] `config/_default/hugo.yaml`, champ `params.description` — remplacer `d'hypnose` par `d’hypnose`.
- [x] `config/_default/hugo.yaml`, champ `markup.goldmark.extensions.footnote.backlinkHTML` — remplacer les délimiteurs simples dans `backlinkHTML: '↩︎'` par des délimiteurs doubles : `backlinkHTML: "↩︎"`.
